{
  "name": "workspace-tabs",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test:ui": "vitest run",
    "test:e2e:prepare": "npm.cmd run build && cargo build --manifest-path ../local-web/Cargo.toml",
    "test:e2e": "npm.cmd run test:e2e:prepare && playwright test",
    "test:e2e:window-width": "powershell.exe -ExecutionPolicy Bypass -File ./scripts/e2e-window-width.ps1",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "@tauri-apps/plugin-opener": "^2",
    "lucide": "^1.24.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@tauri-apps/cli": "^2",
    "jsdom": "^29.1.1",
    "typescript": "~7.0.2",
    "vite": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
